name: 🎮 Manual Import Trigger

on:
  workflow_dispatch:
    inputs:
      import_type:
        description: 'Tipo di import'
        required: true
        default: 'standard'
        type: choice
        options:
          - standard
          - force
          - test
      max_products:
        description: 'Massimo numero di prodotti (default: 10)'
        required: false
        default: '10'
        type: string
      categories:
        description: 'Categorie da importare (separate da virgola)'
        required: false
        default: 'Sistemi di sorveglianza,Reti'
        type: string

jobs:
  manual-import:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📦 Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 📥 Install Dependencies
      run: |
        cd app-senetic-shopify
        npm ci
        
    - name: 🎮 Run Manual Import
      env:
        SENETIC_AUTH: ${{ secrets.SENETIC_AUTH }}
        SHOPIFY_STORE_URL: ${{ secrets.SHOPIFY_STORE_URL }}
        SHOPIFY_ACCESS_TOKEN: ${{ secrets.SHOPIFY_ACCESS_TOKEN }}
        IMPORT_TYPE: ${{ github.event.inputs.import_type }}
        MAX_PRODUCTS: ${{ github.event.inputs.max_products }}
        CATEGORIES: ${{ github.event.inputs.categories }}
      run: |
        cd app-senetic-shopify
        echo "🎮 Import manuale avviato"
        echo "📊 Tipo: $IMPORT_TYPE"
        echo "📦 Max prodotti: $MAX_PRODUCTS"
        echo "📋 Categorie: $CATEGORIES"
        node src/scripts/manual-import.js
        
    - name: 📊 Display Results
      if: always()
      run: |
        echo "📋 Risultati import:"
        if [ -f app-senetic-shopify/logs/manual-import.log ]; then
          tail -20 app-senetic-shopify/logs/manual-import.log
        fi